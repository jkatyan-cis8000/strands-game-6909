# runtime layer

Application lifecycle, orchestration, and dependency wiring.

This layer handles application startup, shutdown, and the wiring
together of all components.

May import from: `types`, `config`, `repo`, `service`, `providers`, `runtime` (self-imports allowed)
