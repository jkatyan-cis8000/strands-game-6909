# utils layer

Pure utility functions and helpers.

This layer contains general-purpose helper functions that don't
depend on any internal modules. No domain logic belongs here.

May import from: `utils` only (self-imports allowed)
