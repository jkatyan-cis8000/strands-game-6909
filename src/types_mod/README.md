# types layer

Pure type definitions for the game. No logic, only data structures.

This layer contains all type definitions used throughout the application.
Types are immutable and represent the canonical data structures for the game.

May import from: `types` only (self-imports allowed)
