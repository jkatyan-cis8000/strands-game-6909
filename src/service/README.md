# service layer

Business logic and core game rules.

This layer contains the main game logic including word validation,
scoring, board generation, and game state management.

May import from: `types`, `config`, `repo`, `providers`, `service` (self-imports allowed)
