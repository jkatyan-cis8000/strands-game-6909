# config layer

Constants, settings, and environment configuration.

This layer defines all configuration values including game constants,
difficulty settings, and environment-specific configurations.

May import from: `types`, `config` (self-imports allowed)
