# repo layer

Data access layer for external state.

This layer handles all data persistence including file I/O, database
operations, and external API calls.

May import from: `types`, `config`, `repo` (self-imports allowed)
